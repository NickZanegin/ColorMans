using UnityEngine;

public static class TryToAccessPath 
{
    public static bool TryAcccess(GwWaypoint[] waipoint)
    {
        for (int i = 0; i < waipoint.Length; i++)
        {
            //if (!waipoint[i].access)
            //{
            //    return false;
            //}
        }
        return true;
    }
}
